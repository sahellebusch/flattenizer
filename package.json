{
  "name": "flattenizer",
  "description": "Small library for flattening and unflattening objects.",
  "version": "2.0.0",
  "author": "Sean Hellebusch <sahellebusch@gmail.com>",
  "bugs": {
    "url": "https://github.com/sahellebusch/flattenizer/issues"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.5",
    "chai": "^4.2.0",
    "gh-pages": "^2.0.1",
    "husky": "^1.1.4",
    "lint-staged": "^8.0.4",
    "mocha": "^5.2.0",
    "ts-mocha": "^2.0.0",
    "ts-node": "^7.0.1",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.16.0",
    "typedoc": "^0.13.0",
    "typescript": "^3.1.6"
  },
  "homepage": "https://github.com/sahellebusch/flattenizer#readme",
  "license": "MIT",
  "lint-staged": {
    "**/*.ts": [
      "tslint --fix",
      "git add"
    ]
  },
  "main": "./dist/flattenizer.min.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sahellebusch/flattenizer.git"
  },
  "scripts": {
    "lint": "tslint src/flattenizer.ts spec/flattenizer.spec.ts",
    "lint:fix": "tslint src/flattenizer.ts spec/flattenizer.spec.ts --fix",
    "build": "tsc",
    "docs:build": "typedoc --out api",
    "test": "ts-mocha -p tsconfig.json spec/**/*.spec.ts",
    "prepublish": "npm run build",
    "postpublish": "npm run docs:deploy",
    "docs:deploy": "npm run docs:build && gh-pages -d api -m \"Update: Docs\""
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "pre-push": "npm test"
    }
  }
}
